# ── Helios quant-engine runtime dependencies ─────────────────────────────────
# Pinned to minimum-compatible majors. The hot numerical paths are numpy and
# polars; scipy is used for optimisation and distributions only.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.8,<3.0
pydantic-settings>=2.4,<3.0
orjson>=3.10,<4.0

# ── Database: SQLAlchemy Core (no ORM — these are analytical queries) ────────
SQLAlchemy>=2.0.30,<2.1
asyncpg>=0.29,<0.31
greenlet>=3.0

# ── Numerics / frames ───────────────────────────────────────────────────────
numpy>=1.26,<3.0
pandas>=2.2,<3.0
polars>=1.0,<2.0
pyarrow>=16.0
scipy>=1.13,<2.0

# ── Object storage (Parquet feature store on S3/MinIO) ──────────────────────
boto3>=1.34,<2.0

# ── Messaging / workers ─────────────────────────────────────────────────────
aiokafka>=0.10,<0.13
celery>=5.4,<6.0
redis>=5.0,<7.0

# ── Observability ───────────────────────────────────────────────────────────
structlog>=24.1
prometheus-client>=0.20
opentelemetry-api>=1.25
opentelemetry-sdk>=1.25
opentelemetry-exporter-otlp-proto-grpc>=1.25
opentelemetry-instrumentation-fastapi>=0.46b0
opentelemetry-instrumentation-asyncpg>=0.46b0
